schema {
    query: Query
   # mutation: Mutation
}

type Query {
    human(id: ID!): Human
}

#type Mutation{

#}

interface Character{
    id: ID!
    name: String!
    friends: [Character]
}

type Human implements Character {
    id: ID!
    name: String!
    friends: [Character]
}